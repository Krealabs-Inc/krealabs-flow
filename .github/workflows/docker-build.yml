name: Docker Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          build-args: |
            NEXT_PUBLIC_STACK_PROJECT_ID=00000000-0000-0000-0000-000000000000
            NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=pck_ci_placeholder_000000000000000000000000000
            STACK_SECRET_SERVER_KEY=ssk_ci_placeholder_000000000000000000000000000
          cache-from: type=gha
          cache-to: type=gha,mode=max
